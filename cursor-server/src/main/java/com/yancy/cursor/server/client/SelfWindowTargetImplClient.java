package com.yancy.cursor.server.client;

/**
 * 当前服务器 WindowClient类
 * @author yancy0109
 */
public class SelfWindowTargetImplClient extends WindowClient implements SelfWindowTarget {
    public SelfWindowTargetImplClient(ClientObject clientObject) {
        super(clientObject);
    }
}
